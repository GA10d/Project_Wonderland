# 熔岩魔王官邸显示排查

2026-09-10（America/New_York）。用户请求：「生成一充满岩浆的火山场景，中间有一恶魔居住的火山官邸，进入之后是一个魔王的王宫」。

任务 `9d59c62ad0c30921` 于 20:12:52 开始，20:16:28 完成，总耗时约 216.85 秒。日志中有一次场景规划、三次生图、三次视觉检查，三项素材均通过，无失败事件。最终状态是 `complete`，世界文件为 `worlds/9a59e2b1a0a8/world.json`，标题为「熔岩魔王官邸」。原任务和生成素材未重新生成或覆盖。

实测从保存文件可以渲染城堡、岩浆与王座内饰，进出往返和碰撞检查通过。因此，不能把没有看到结果解释为生成任务失败。

界面代码存在历史列表仅在启动和当前实例生成完成时刷新的缺口：一个已打开的实例不会发现其他实例或命令行刚保存的结果。另外，选择历史场景后，日志路径仍会被上一次 Generator 对象覆盖。修复为两秒检查目录变化、F5 手动刷新、提示新场景，以及让日志关联选中的场景。加载失败会单独提示并记录，而不会将已保存的生成结果误报为失败。

旧日志只记录生成流程，没有当时窗口的选择/加载信息；无法仅从旧日志确证用户当时查看的是哪一窗口、哪一场景。新 `logs/studio.jsonl` 增加实例 PID、运行 ID、列表刷新和场景加载事件，以便后续精确定位。

验证：26 项测试通过；新增测试复现了「窗口启动后另一个进程保存场景」和「旧 Generator 覆盖新选择的日志」两种情况。实际城堡的预览、日志关联及进出交互检查通过。
